import XCTest
@testable import mxfitness_model

final class mxfitness_modelTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(mxfitness_model().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
